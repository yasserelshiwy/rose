export const metadata = {
  title: "Rose-productDetails",
  description: "productDetails page for rose e-commerce",
};

export default function productDetailsLayout({ children }) {
  return <>{children}</>;
}
