export const metadata = {
  title: "Rose-Change Password",
  description: "changePassword page for rose e-commerce",
};

export default function changePasswordLayout({ children }) {
  return <>{children}</>;
}
