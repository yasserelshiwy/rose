export const metadata = {
  title: "Rose-Forget Password",
  description: "forgetPassword page for rose e-commerce",
};

export default function forgetPasswordLayout({ children }) {
  return <>{children}</>;
}
